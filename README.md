Bot Name
========

Short Description

### How to start

1. Fill the `settings.toml` file

2. Install dependencies

```shell
pip install -r requirements.txt
```

3. Run the bot

```shell
python -m mybot
```